{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-xs-2"></div>
        <div class="col-xs-8">
            <h1>Create new Address</h1>
            {{ form_start(addressEntry) }}
            {{ form_widget(addressEntry) }}
            {{ form_end(addressEntry) }}
        </div>
        <div class="col-xs-2"></div>
    </div>

{% endblock %}

{% block stylesheets %}
<style>
    body {
        padding-top: 50px;
    }
    .starter-template {
        padding: 40px 15px;
        text-align: center;
    }
</style>
{% endblock %}
